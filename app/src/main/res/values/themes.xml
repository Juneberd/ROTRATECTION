<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="Base.Theme.AppCompat.Light.DarkActionBar">

        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <style name="AppBarOverlay" parent="Base.Theme.AppCompat.Light.DarkActionBar" />

    <style name="PopupOverlay" parent="Base.Theme.AppCompat.Light.DarkActionBar" />

    <style name="Theme.ROTRATECTIONV4.Fullscreen" parent="Theme.ROTRATECTIONV4">
        <item name="android:actionBarStyle">
            @style/Widget.Theme.ROTRATECTIONV4.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.ROTRATECTIONV4.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>